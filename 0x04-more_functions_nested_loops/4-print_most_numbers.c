#include <stdio.h>
#include "main.h"

/**
 * print_most_numbers - prints numbers
 *
 * Return: 0
 */

void print_most_numbers(void)
{
	int z;

	if ((z >= 0) && (z <= 9))
	{
		if ((z == 2) && (z == 4))
		{

		}
		else
		_putchar(z);
	}
	_putchar('\n');
}
