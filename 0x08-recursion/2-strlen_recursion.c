#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strlen_recursion - counting strings using recursion
 * @s: string pointer
 *
 * Return: strlen
 */

int _strlen_recursion(char *s)
{
	size_t strlen(const char *s);
	return (strlen(s));
}
