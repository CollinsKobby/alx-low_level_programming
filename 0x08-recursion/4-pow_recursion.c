#include <stdio.h>
#include <math.h>
#include "main.h"

/**
 * _pow_recursion - power of a number
 * @x: number
 * @y: power
 *
 * Return: pow()
 */

int _pow_recursion(int x, int y)
{
	double pow(double x, double y);
	if (y < 0)
	{
		return (-1);
	}

	return (pow(x, y));
}
