#include <stdio.h>
#include "main.h"

/**
 * _puts_recursion - recursive function of puts
 *
 * @s: charcater pointer
 * Return: puts(s)
 */

void _puts_recursion(char *s)
{
	int puts(const char *s);

	return (puts(s));
}
