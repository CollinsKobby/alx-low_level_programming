#include <stdio.h>
#include <string.h>

/**
 * _strlen - counts the number of strings
 *
 * @s: character pointer
 *
 * Return: strlen(s)
 */

int _strlen(char *s)
{
	strlen(s);

	return (strlen(s));
}

