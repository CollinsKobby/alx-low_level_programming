#include <string.h>
#include <stdio.h>
#include "main.h"

/**
 * _strstr - finds first occurence
 *
 * @haystack: first member
 * @needle: second member
 *
 * Return: strstr()
 */

char *_strstr(char *haystack, char *needle)
{
	char *strstr(const char *haystack, const char *needle);

	return (strstr(haystack, needle));
}

