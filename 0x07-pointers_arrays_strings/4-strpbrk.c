#include <string.h>
#include <stdio.h>
#include "main.h"

/**
 * _strpbrk - locates first occurence in the string
 *
 * @s: first member
 * @accept: second member
 *
 * Return: strpbrk()
 */

char *_strpbrk(char *s, char *accept)
{
	char *strpbrk(const char *s, const char *accept);

	return (strpbrk(s, accept));
}
